---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by Administrator.
--- DateTime: 2018/8/9 16:44
---
CannotDropItemConfig = {}
CannotDropItemConfig.CannotDropItem = {}

function CannotDropItemConfig:init(Item)
    for ItemIndex, element in pairs(Item) do
        local item = {}
        item.itemId = tonumber(element.itemId)
        self.CannotDropItem[ItemIndex] = item
    end
end

function CannotDropItemConfig:isCannotDrop(itemId)
    for _, item in pairs(self.CannotDropItem) do
        if item.itemId == itemId then
            return true
        end
    end
    return false
end

return CannotDropItemConfig