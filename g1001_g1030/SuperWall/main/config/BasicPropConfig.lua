---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by Administrator.
--- DateTime: 2018/8/16 20:05
---

BasicPropConfig = {}
BasicPropConfig.items = {}

function BasicPropConfig:init(config)
    self:initBasicProp(config)
end

function BasicPropConfig:initBasicProp(Props)
    for index, prop in pairs(Props) do
        local props={}
        props.id=tonumber(prop.id)
        props.meta=tonumber(prop.meta)
        props.num = tonumber(prop.num)
        self.items[tonumber(index)]=props
    end
end

return BasicPropConfig
