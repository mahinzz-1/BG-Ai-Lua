---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by Jimmy.
--- DateTime: 2019/1/29 0029 11:48
---
local World

EngineWorld = {}

function EngineWorld:setWorld(world)
    World = world
end

function EngineWorld:getWorld()
    return World
end

function EngineWorld:stopWorldTime(time)
    World:setWorldTime((time or 6000))
    World:setTimeStopped(true)
end

function EngineWorld:startWorldTime(time)
    World:setTimeStopped(false)
    World:setWorldTime((time or 6000))
end

function EngineWorld:setQuickWorldTime(ticks, speed, endTime)
    World:setQuickWorldTime(ticks, speed, endTime)
end

function EngineWorld:isNight()
    local time = tonumber(tostring(World:getWorldTime()))
    time = time % 24000
    return time >= 12000
end

function EngineWorld:resetMap()
    World:resetMap()
end

function EngineWorld:setBlockToAir(blockPos)
    World:setBlockToAir(blockPos)
end

function EngineWorld:setBlock(blockPos, id, meta, flag, immediate)
    if immediate == nil then
        immediate = true
    end
    World:setBlock(blockPos, id, (meta or 0), (flag or 0), immediate)
end

function EngineWorld:getBlockId(blockPos)
    return World:getBlockId(blockPos)
end

function EngineWorld:getBlockMeta(blockPos)
    return World:getBlockMeta(blockPos)
end

function EngineWorld:getEntity(entityId)
    return World:getEntity(entityId)
end

function EngineWorld:removeEntity(entityId)
    World:removeEntity(entityId)
end

function EngineWorld:addEntityItem(itemId, count, meta, life, pos, motion, isCombine, isEverLife)
    motion = (motion or VectorUtil.ZERO)
    isCombine = (isCombine or false)
    isEverLife = (isEverLife or false)
    return World:spawnItemInWorld(itemId, count, meta, life, pos, motion, isCombine, isEverLife)
end

function EngineWorld:addRankNpc(position, yaw, name)
    return World:addRankNpc(position, yaw, name or "")
end

function EngineWorld:addMerchantNpc(position, yaw, name)
    return World:addMerchant(position, yaw, name or "")
end

function EngineWorld:addVehicleNpc(position, type, yaw)
    return World:addVehicle(position, type, yaw)
end

function EngineWorld:addRobot(position, id, yaw, actor)
    return World:addRobot(position, id, yaw, actor)
end

function EngineWorld:addCreatureNpc(position, id, yaw, actor, creator)
    local entity = World:addCreature(position, id, yaw, actor or "")
    if creator ~= nil then
        creator(entity)
    end
    World:spawnEntityInWorld(entity)
    return entity:hashCode()
end

function EngineWorld:addActorNpc(position, yaw, actor, creator)
    local entity = World:addActorNpc(position, yaw, actor or "")
    if creator ~= nil then
        creator(entity)
    end
    World:spawnEntityInWorld(entity)
    return entity:hashCode()
end

function EngineWorld:addSessionNpc(position, yaw, type, actor, creator)
    local entity = World:addSessionNpc(position, yaw, type, actor or "")
    if creator ~= nil then
        creator(entity)
    end
    World:spawnEntityInWorld(entity)
    return entity:hashCode()
end

function EngineWorld:addFlyingMount(position, yaw, settingId, creator)
    local entity = World:addFlyingMount(position, yaw, settingId)
    if creator ~= nil then
        creator(entity)
    end
    World:spawnEntityInWorld(entity)
    return entity:hashCode()
end

function EngineWorld:addFloatingMount(position, yaw, settingId, creator)
    local entity = World:addFloatingMount(position, yaw, settingId)
    if creator ~= nil then
        creator(entity)
    end
    World:spawnEntityInWorld(entity)
    return entity:hashCode()
end

function EngineWorld:addSkillThrowable(player, creator)
    local entity = World:addSkillThrowable(player.entityPlayerMP)
    if creator ~= nil then
        creator(entity)
    end
    World:spawnEntityInWorld(entity)
    return entity:hashCode()
end

function EngineWorld:addRailCar(position, yaw, actor, isRun, creator)
    local entity = World:addRailCar(position, yaw, actor, isRun)
    if creator ~= nil then
        creator(entity)
    end
    World:spawnEntityInWorld(entity)
    return entity:hashCode()
end

function EngineWorld:addCreatureBullet(m_throwerId, targetId, creator)
    local entity = World:addCreatureBullet(m_throwerId, targetId)
    if creator ~= nil then
        creator(entity)
    end
    World:spawnEntityInWorld(entity)
    return entity:hashCode()
end

function EngineWorld:addEntityPlayerAI(initPos, name, rakssid, creator)
    local entity = World:addEntityPlayerAI(initPos, name, rakssid)
    if creator ~= nil then
        creator(entity)
    end
    World:spawnEntityInWorld(entity)
    return entity:hashCode()
end

function EngineWorld:addSimpleEffect(name, position, yaw, duration, targetId, scale)
    duration = (duration or -1)
    targetId = (targetId or 0)
    scale = (scale or 1)
    World:addSimpleEffect(name, position, yaw, duration, targetId, scale)
end

function EngineWorld:createSchematic(schematicName, startPos, reversalX, reversalZ, coverOldBlock, placeRakssid, placeBlockId, placeOffset, extraParam)
    reversalX = reversalX or false
    reversalZ = reversalZ or false
    if coverOldBlock == nil then
        coverOldBlock = true
    else
        coverOldBlock = false
    end
    placeRakssid = placeRakssid or 0
    placeBlockId = placeBlockId or 0
    placeOffset = placeOffset or 0
    extraParam = extraParam or 0
    World:createSchematic(schematicName, startPos, placeRakssid, reversalX, reversalZ, coverOldBlock, placeBlockId, placeOffset, extraParam)
end

function EngineWorld:destroySchematic(schematicName, startPos, reversalX, reversalZ, placeRakssid, placeOffset, extraParam)
    reversalX = reversalX or false
    reversalZ = reversalZ or false
    placeRakssid = placeRakssid or 0
    placeOffset = placeOffset or 0
    extraParam = extraParam or 0
    World:destroySchematic(schematicName, startPos, placeRakssid, reversalX, reversalZ, placeOffset, extraParam)
end

function EngineWorld:isBlockChange(blockPos)
    return World:isInChangeRecord(blockPos)
end

function EngineWorld:changePlayerActor(player, actor, bodyId)
    if player.entityPlayerMP == nil then
        return
    end
    World:changePlayerActor(player.entityPlayerMP, actor, bodyId or "")
end

function EngineWorld:restorePlayerActor(player)
    if player.entityPlayerMP == nil then
        return
    end
    World:restorePlayerActor(player.entityPlayerMP)
end

function EngineWorld:addGoodsGroupToShop(type, icon, name)
    World:addGoodsGroupToShop(type, icon or "", name or "")
end

function EngineWorld:addGoodsToShop(type, goods)
    World:addGoodsToShop(type, goods)
end

return EngineWorld