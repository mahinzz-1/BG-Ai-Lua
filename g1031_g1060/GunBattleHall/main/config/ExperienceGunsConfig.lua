---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by Administrator.
--- DateTime: 2018/9/12 17:44
---
require "data.GameExGunNpc"

ExperienceGunsConfig = {}
ExperienceGunsConfig.exGuns = {}

function ExperienceGunsConfig:init(exGuns)
    self:initExGuns(exGuns)
end

function ExperienceGunsConfig:initExGuns(ExGuns)
    for index, exGun in pairs(ExGuns) do
        local data = {}
        data.itemId = tonumber(exGun.itemId)
        data.actor = exGun.actor
        data.x = tonumber(exGun.x)
        data.y = tonumber(exGun.y)
        data.z = tonumber(exGun.z)
        data.yaw = tonumber(exGun.yaw)
        data.gunName = exGun.gunName
        data.itemNum = tonumber(exGun.itemNum)
        data.isGun = tonumber(exGun.isGun)
        data.isSprayPaint = tonumber(exGun.isSprayPaint)
        data.buyHint = exGun.buyHint
        self.exGuns[tonumber(index)] = data
        GameExGunNpc.new(data)
    end
end

function ExperienceGunsConfig:getExGunByItemId(itemId)
    for i, exGun in pairs(self.exGuns) do
        if exGun.itemId == itemId then
            return exGun
        end
    end
end

return ExperienceGunsConfig