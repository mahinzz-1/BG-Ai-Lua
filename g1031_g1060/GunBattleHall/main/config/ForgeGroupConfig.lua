---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by Administrator.
--- DateTime: 2018/9/26 17:07
---
ForgeGroupConfig = {}
ForgeGroupConfig.groups = {}

function ForgeGroupConfig:init(groups)
    self:initForgeGroup(groups)
end

function ForgeGroupConfig:initForgeGroup(groups)
    for _, group in pairs(groups) do
        local item = {}
        item.id = group.id
        item.name = group.name
        table.insert(self.groups, item)
    end
    table.sort(self.groups, function(a, b)
        return tonumber(a.id) < tonumber(b.id)
    end)
end

