---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by Administrator.
--- DateTime: 2019/1/12 15:36
---
HonorExponentConfig = {}
HonorExponentConfig.exponents = {}

function HonorExponentConfig:init(config)
    self:initHonorExponent(config)
end

function HonorExponentConfig:initHonorExponent(config)
    for _, exponent in pairs(config) do
        local item = {}
        item.id = tonumber(exponent.id)
        item.name = exponent.name
        item.rate = exponent.rate
        self.exponents[item.id] = item
    end
end

function HonorExponentConfig:getRateById(id)
    for _, exponent in pairs(self.exponents) do
        if id == exponent.id then
            return exponent.rate
        end
    end
    return 1
end

return HonorExponentConfig