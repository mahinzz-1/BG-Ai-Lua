---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by Administrator.
--- DateTime: 2019/10/17 22:43
---

AppPropConfig = {}
AppPropConfig.AppProps = {}

function AppPropConfig:init(config)
    for _, item in pairs(config) do
        local pItem = {}
        pItem.id = item.id
        pItem.itemId = item.itemId
        pItem.type = tonumber(item.type)
        pItem.itemMeta = tonumber(item.itemMeta)
        pItem.itemNum = tonumber(item.itemNum)
        pItem.enchants = item.enchants
        pItem.effects = item.effects
        pItem.extraHp = item.extraHp
        pItem.isRepeat = tonumber(item.isRepeat)
        self.AppProps[item.itemId] = pItem
    end
end