---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by Yanzhuolin.
--- DateTime: 2019/4/15 14:29
---

SignInConfig = {}
SignInConfig.List = {}

function SignInConfig:initConfig(configs)
    for _, config in pairs(configs) do
        local item = {
            Id = tonumber(config.Id),
            Order = tonumber(config.Order),
            ShowFrom= tonumber(config.ShowFrom),
            RewardId = tonumber(config.RewardId)
        }
        self.List[item.Id] = item
    end
end

function SignInConfig:getConfigById(id)
    return self.List[id]
end

function SignInConfig:getNextSignInConfig(id)
    local config = self:getConfigById(id)
    if config then
        local nextOrder = config.Order + 1
        for _, next in pairs(self.List) do
            if next.Order == nextOrder then
                return next
            end
        end
    end
    return nil
end

return SignInConfig