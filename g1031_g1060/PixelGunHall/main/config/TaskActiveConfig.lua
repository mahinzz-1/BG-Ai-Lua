---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by Jimmy.
--- DateTime: 2019/3/8 0008 15:10
---
TaskActiveConfig = {}
TaskActiveConfig.TaskActives = {}

function TaskActiveConfig:initConfig(actives)
    for _, config in pairs(actives) do
        local item = {}
        item.Id = tonumber(config.Id)
        item.Type = tonumber(config.Type)
        item.ActiveValue = tonumber(config.ActiveValue)
        item.RewardIds = StringUtil.split(config.RewardIds, "#")
        table.insert(self.TaskActives, item)
    end
end

function TaskActiveConfig:getTaskActive(activeId)
    for _, active in pairs(self.TaskActives) do
        if active.Id == activeId then
            return active
        end
    end
end

return TaskActiveConfig