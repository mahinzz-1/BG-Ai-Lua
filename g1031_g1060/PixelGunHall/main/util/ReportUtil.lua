---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by Yanzhuolin.
--- DateTime: 2019/4/2 12:05
---

ReportUtil = {}

function ReportUtil:reportUpgrade(player)
    local parts = { "Upgrade", "PlayerLv" .. player.level, "ArmorLv" .. player.armor_level }
    GameAnalytics:design(player.userId, player.armor_level, parts)
end

function ReportUtil:reportOpenChest(player, chestId)
    local parts = { "OpenChest", "Chest" .. chestId, "PlayerLv" .. player.level }
    GameAnalytics:design(player.userId, player.level, parts)
end

function ReportUtil:reportActiveReceive(player, activeId)
    local parts = { "ActiveReceive", "Active" .. activeId, "PlayerLv" .. player.level }
    GameAnalytics:design(player.userId, player.level, parts)
end

function ReportUtil:reportTaskReceive(player, taskId)
    local taskName = TaskReportConfig:getNameByTaskId(taskId)
    if taskName then
        local parts = { "TaskReceive", taskName, "PlayerLv" .. player.level }
        GameAnalytics:design(player.userId, player.level, parts)
    end
end

function ReportUtil:reportSignIn(player, signId)
    local parts = { "SignIn", "SignIn" .. signId, "PlayerLv" .. player.level }
    GameAnalytics:design(player.userId, player.level, parts)
end

return ReportUtil