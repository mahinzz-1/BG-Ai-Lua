---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by xiaolai.
--- DateTime: 2019/10/17 17:21
---

NotificationConfig = {}
NotificationConfig.notifications = {}

function NotificationConfig:initConfig(config)
    for _, notification in pairs(config) do
        table.insert(self.notifications, json.encode({ message = notification.message }))
    end
end

function NotificationConfig:onPlayerReady(player)
    for _, notification in pairs(self.notifications) do
        HostApi.addDefaultNotification(player.rakssid, notification)
    end
end

return NotificationConfig